const COLORS = {
  white: "#fff",
  first: "#47BFDF",
  second: "#4A91FF",
  secondary: "#444E72",
  tertiary: "#838BAA",
};

const LIGHT_SHADOW = {
  textShadowColor: "rgba(0, 0, 0, 0.2)",
  textShadowOffset: { width: -2, height: 3 },
  textShadowRadius: 1,
  fontFamily: "Overpass_Reg",
  color: COLORS.white,
};

const BOLD_SHADOW = {
  textShadowColor: "rgba(0, 0, 0, 0.2)",
  textShadowOffset: { width: -2, height: 3 },
  textShadowRadius: 1,
  fontFamily: "Overpass_Bold",
  color: COLORS.white,
};

export { COLORS, LIGHT_SHADOW, BOLD_SHADOW };
